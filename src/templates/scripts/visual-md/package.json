{
  "name": "visual-md-skill",
  "version": "0.1.0",
  "private": true,
  "description": "Local skill: iterative md editing via html canvas with multi-scope widgets",
  "scripts": {
    "test": "node --test test/*.cjs"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  }
}
